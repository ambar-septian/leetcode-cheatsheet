func fn(_ arr1: [Int], _ arr2: [Int]) -> Int {
    var i = 0
    var j = 0
    var ans = 0

    while i < arr1.count && j < arr2.count {
        // TODO: logic
        let CONDITION = true
        if CONDITION {
            i += 1
        } else {
            j += 1
        }
    }

    while i < arr1.count {
        // TODO: logic
        i += 1
    }

    while j < arr2.count {
        // TODO: logic
        j += 1
    }

    return ans
}
