func fn(_ arr: [Int]) -> Int {
    var ans = 0
    var left = 0
    var right = arr.count - 1

    while left < right {
        // TODO: logic with left and right
        let CONDITION = true
        if CONDITION {
            left += 1
        } else {
            right -= 1
        }
    }

    return ans
}
